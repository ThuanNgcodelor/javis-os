#!/bin/bash
# ==============================================================================
# Click đúp để khởi động toàn bộ JAVIS OS (Mở 4 cửa sổ Terminal riêng biệt)
# ==============================================================================
cd "$(dirname "$0")" || exit 1
bash "bin/start-all.sh"
